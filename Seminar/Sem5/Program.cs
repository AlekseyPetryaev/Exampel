// Задайте масим из 12 чисел положительны и отрицательных чисел 
Console.Clear();
/*
int [] GetArray(int size, int minValue, int maxValue)
{
    int[] res = new int [size];

    for (int i = 0; i<size; i++)
    {
        res [i] new Random().Next(minValue, maxValue +1);

    } 
    return res;
}

int [] array =GetArray(12, -9, 9);
Console.WriteLine(String.Join(" ", array));

int positiveSum = 0;
int negatevrSum = 0;
foreach (int el in array)
{
    if (el > 0)
    { 
        positiveSum += el;
    }
    else    
    {
        negatevrSum += el;
    }

     //positiveSum += el > 0 ? el :0; //тернарный  аператор
     //negatevrSum += el < 0 ? el :0;
}
*/

/*
int [] GetArray(int size, int minValue, int maxValue)
{
    int[] res = new int [size];

    for (int i = 0; i<size; i++)
    {
        res [i] new Random().Next(minValue, maxValue +1);
    } 
    return res;
}

void Convert(int[] array)
{
    for (int = 0; i < array.Length; i++)
    {
        array[i] -array[i];
    }   
}

Convert (array);
Convert.WriteLine
*/


